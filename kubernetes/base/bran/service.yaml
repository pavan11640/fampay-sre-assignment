apiVersion: v1
kind: Service
metadata:
  name: bran
  namespace: fampay
  labels:
    app: bran
spec:
  type: ClusterIP
  selector:
    app: bran
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
