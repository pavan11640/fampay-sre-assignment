apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-bran-config
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
